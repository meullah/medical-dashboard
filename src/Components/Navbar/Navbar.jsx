import React from "react";
import { AppBar, Toolbar } from "@material-ui/core";

const Navbar = props => {
  return (
    <>
      <AppBar>
        <Toolbar></Toolbar>
      </AppBar>
    </>
  );
};
export default Navbar;
